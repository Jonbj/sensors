# Bootstrap iniziale (una tantum)

Questo documento descrive come creare la struttura iniziale del progetto.

Da usare SOLO la prima volta, su una VM vuota.

```bash
chmod +x bootstrap_vlnet_infra.sh
./bootstrap_vlnet_infra.sh
